package com.webuy.whale.defend.admin.dao.mapper;

import com.webuy.whale.defend.admin.entity.${classInfo.className}PO;
import com.webuy.whale.defend.admin.query.${classInfo.className}Query;

/**
* @description ${classInfo.classComment}
* @author ${authorName}
* @date ${.now?string('yyyy-MM-dd')}
*/
public interface ${classInfo.className}Mapper extends MyBatisBaseMapper<${classInfo.className}PO, ${classInfo.className}Query> {

}
